import * as rewire from "rewire";
import * as assert from "assert";

describe('TestWelcomeSceneAdd', function () {
    it('', function () {
        let result = 10 + 9;
        assert.equal(result, 19)
    });
});

